[tasks.setup]
description = "One-time: download Sublime's syntax_tests binary and link this package"
run         = "./scripts/setup_syntax_tests.sh"

[tasks.gen-tests]
description = "Convert tests/*.yaml into package/tests/syntax_test_* (standalone, no binary needed)"
run         = "./scripts/yaml2syntaxtest.py tests package/tests"

[tasks.test]
description = "Generate + run syntax tests"
run         = "./scripts/run_tests.sh"
